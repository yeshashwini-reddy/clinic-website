{
  "name": "careplus-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Root package to run backend and frontend together",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "fast": "npm run fast --prefix frontend",
    "start": "npm run fast --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "ngrok": "^5.0.0-beta.2"
  }
}
